type PubM {
  login (username: ID!, password: String!): TokenAndError!
  signup (
    username: ID!,
    password: String!,
    email: String!,
    firstName: String,
    lastName: String
  ): UserAndError
}
